import { defineConfig } from "tsup";

export default defineConfig({
  entry: [
    "src/index.ts",
    "src/lib/color/index.ts",
    "src/lib/system/index.ts",
    "src/lib/default/index.ts",
  ],
  splitting: true,
  sourcemap: true, 
  outExtension({ format }) {
    return {
      js: `.${format}.js`,
    }
  },
  shims:true,
  clean: true,
  format: ['cjs','esm','iife']
});
