"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nearestColor = exports.distance = void 0;
const to_1 = require("./to");
const distance = (a, b) => Math.sqrt(Math.pow(a.r - b.r, 2) + Math.pow(a.g - b.g, 2) + Math.pow(a.b - b.b, 2));
exports.distance = distance;
const nearestColor = (color, colors) => colors
    .map((c) => ({ color: c, distance: (0, exports.distance)((0, to_1.toRGB)(color), (0, to_1.toRGB)(c)) }))
    .sort((a, b) => a.distance > b.distance ? 1 : b.distance > a.distance ? -1 : 0)[0].color;
exports.nearestColor = nearestColor;
//# sourceMappingURL=nearest.js.map