"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const helpers_1 = require("./helpers");
describe("isBetween", () => {
    it("Should be a number between 1 and 10", () => {
        expect((0, helpers_1.isBetween)(5, 0, 10)).toBe(true);
        expect((0, helpers_1.isBetween)(0, 0, 10)).toBe(true);
        expect((0, helpers_1.isBetween)(10, 0, 10)).toBe(true);
    });
    it("Should not be a number between 1 and 10", () => {
        expect((0, helpers_1.isBetween)(11, 0, 10)).toBe(false);
        expect((0, helpers_1.isBetween)(-1, 0, 10)).toBe(false);
    });
});
describe("areBetween", () => {
    it("Should be a numbers between 1 and 10", () => {
        expect((0, helpers_1.areBetween)([0, 5, 10], 0, 10)).toBe(true);
    });
    it("Should not be a number between 1 and 10", () => {
        expect((0, helpers_1.areBetween)([-1, 11], 0, 10)).toBe(false);
    });
    it("Should not be a number between 1 and 10 - one odd", () => {
        expect((0, helpers_1.areBetween)([1, 5, 11], 0, 10)).toBe(false);
    });
});
describe("shift", () => {
    it("Should return the array without the first element", () => {
        expect((0, helpers_1.shift)([0, 1, 2])).toEqual([1, 2]);
    });
    it("Should return an empty array, because the array was only one item", () => {
        expect((0, helpers_1.shift)([0])).toEqual([]);
    });
});
//# sourceMappingURL=helpers.test.js.map