import { LoggerSettings } from "../types";
import { BlockType } from "./blocks.model";
export declare const createBlock: (blockType: BlockType, content: any, settings?: Partial<LoggerSettings>) => Promise<string[]>;
export declare const block: (blockType: BlockType, content: any, settings?: Partial<LoggerSettings>) => Promise<void>;
